pandas>=2.0
numpy>=1.24
Pillow>=10.0
python-dateutil>=2.8
requests>=2.31
flask>=3.0
waitress>=3.0
openpyxl>=3.1
psycopg[binary]>=3.1
psycopg_pool>=3.2
SQLAlchemy>=2.0
# Source DB driver for ingestion: psycopg (above) covers PostgreSQL sources.
# For a MySQL source, also install PyMySQL (pip install pymysql) and use a
# mysql+pymysql://… SOURCE_DATABASE_URL.
# Legal workspace PDF generation (optional — fallback available without it):
reportlab>=4.0
# SARFAESI Legal Workspace: triple-route OCR + document extraction
PyMuPDF>=1.23
rapidocr-onnxruntime>=1.3
# Medha and Gemini are both called over plain HTTP (workspaces/legal/llm.py) — no Google SDK
httpx>=0.27
