pandas>=2.0
numpy>=1.24
Pillow>=10.0
python-dateutil>=2.8
requests>=2.31
flask>=3.0
waitress>=3.0
openpyxl>=3.1
psycopg[binary]>=3.1
psycopg_pool>=3.2
SQLAlchemy>=2.0
# Source DB driver for ingestion: psycopg (above) covers PostgreSQL sources.
# For a MySQL source, also install PyMySQL (pip install pymysql) and use a
# mysql+pymysql://… SOURCE_DATABASE_URL.
